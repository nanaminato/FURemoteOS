# AGENTS.zh-CN.md（智能体开发规范）

> 适用于 Codex、ChatGPT、Claude Code、Gemini CLI、Cursor 及其他编码智能体在 RemoteOS Flutter 项目中的工作指引。
>
> [English version](AGENTS.md)

***

## 1. 任务目标

你的任务是安全、渐进地修改 RemoteOS Flutter 客户端。

RemoteOS 是一款桌面服务器管理应用。

在本仓库工作时，应优先优化以下方面：

```text
正确性
行为兼容性
小型、可审查的变更
架构合规性
安全性
可维护性
智能体易读的代码
```

不要为了在最短时间内产出最多代码而牺牲以上原则。

***

## 2. 必读文档

在进行非平凡的变更之前，请先阅读：

```text
[ARCHITECTURE.md](docs/architecture/ARCHITECTURE.md)
[MIGRATION_RULES.md](docs/MIGRATION_RULES.md)
[AGENTS.md](AGENTS.md) / [AGENTS.zh-CN.md](AGENTS.zh-CN.md)
```

对于迁移任务，还需检查现有 Avalonia 客户端中的对应实现。

[ARCHITECTURE.md](docs/architecture/ARCHITECTURE.md) 中的架构规则是强制要求的，除非任务明确覆盖它们。

[MIGRATION\_RULES.md](docs/MIGRATION_RULES.md) 中的迁移规则对 Avalonia→Flutter 的工作是强制要求的。

***

## 3. 仓库心智模型

期望的依赖流向：

```text
视图（View）
  ↓
视图模型（ViewModel）
  ↓
用例（UseCase，可选）
  ↓
仓库（Repository）
  ↓
服务（Service）
  ↓
RemoteOS.Server / 平台 API
```

期望的生命周期流向：

```text
应用程序（Application）
  ↓
窗口（Window）
  ↓
服务器会话（ServerSession）
  ↓
功能模块（Feature）
```

不要引入会反转这些箭头的依赖关系。

***

## 4. 编辑前准备

在修改代码之前：

1. 定位功能模块负责人；
2. 完整阅读相关的视图（View）代码；
3. 阅读相关的视图模型（ViewModel）代码；
4. 检查仓库/服务依赖；
5. 检查相关对话框和上下文菜单；
6. 检查本地化键；
7. 检查主题使用；
8. 检查测试；
9. 若为迁移任务，检查 Avalonia 源代码；
10. 识别最小的、内聚的变更范围。

当周边逻辑会产生影响时，不要仅基于搜索片段去修补文件。

***

## 5. 不要臆测现有行为

如果任务涉及已有功能：

```text
先搜索
先阅读
先理解
后编辑
```

不要自行臆造：

* API 端点；

* 本地化键；

* 权限名称；

* DTO 字段；

* 路由 ID；

* 功能行为；

* 主题令牌。

优先使用已有的名称和约定。

***

## 6. 变更范围

保持变更范围狭窄且聚焦。

如果被要求迁移或修复某个功能，不要趁机做以下事情：

* 重新设计不相关的界面；

* 重命名不相关的文件；

* 修改不相关的 API；

* 替换状态管理方案；

* 格式化整个仓库；

* 执行大规模依赖升级。

如果需要进行更大规模的重构，请在变更描述中明确说明。

***

## 7. 文件大小规则

推荐：

```text
< 300 行/文件
```

需要仔细审查：

```text
300–500 行/文件
```

重构信号：

```text
> 500 行/文件
```

强烈架构异味：

```text
> 800 行/文件
```

不要创建数千行的 Flutter 页面文件。

按语义职责拆分。

好的示例：

```text
file_manager_view.dart
file_manager_view_model.dart
file_toolbar.dart
file_path_bar.dart
file_list.dart
file_context_menu.dart
rename_dialog.dart
file_repository.dart
remote_file_service.dart
```

避免无意义的碎片化。

除非它们代表真正可复用的组件，否则不要创建类似以下的文件：

```text
file_title_text.dart
file_padding.dart
one_icon_button.dart
```

***

## 8. 视图（View）规则

视图可以处理：

```text
布局
焦点
动画
滚动
鼠标位置
拖拽几何
窗口几何
视觉合成
主题/本地化查找
```

视图不得直接：

```text
调用 HTTP
调用 WebSocket API
调用仓库
实现权限规则
执行业务验证
映射 DTO
持久化设置
```

UI 事件通常应调用视图模型（ViewModel）的命令。

***

## 9. 视图模型（ViewModel）规则

视图模型拥有呈现逻辑。

允许：

```text
可观察状态（observable state）
命令（commands）
选择状态
过滤器
加载状态
错误状态
仓库调用
用例调用
对话框状态
呈现决策
```

禁止：

```text
BuildContext
Widget
Navigator
showDialog()
Theme.of()
MediaQuery
RenderObject
OverlayEntry
```

如果你在 ViewModel 中需要 `BuildContext`，设计很可能是错误的。

***

## 10. 命令（Command）规则

对有意义的用户意图使用命令。

示例：

```text
refreshCommand
connectCommand
saveCommand
deleteCommand
renameCommand
restartServiceCommand
renewCertificateCommand
```

当异步执行状态很重要时，优先使用命令。

不要为纯粹的 UI 切换（如悬停或局部展开）创建命令，除非该状态具有实际的功能意义。

***

## 11. 仓库（Repository）规则

视图模型依赖于仓库接口或用例。

仓库可以：

```text
缓存
合并 REST 和 WebSocket 状态
映射 DTO
规范化错误
协调刷新
提供流（streams）
```

仓库不得导入功能部件（widgets）。

不要从视图或普通视图模型中直接调用原始 API 客户端。

***

## 12. 服务（Service）规则

服务是 I/O 适配器。

示例：

```text
HTTP
WebSocket
文件系统
安全存储
平台通道（platform channel）
窗口系统
```

服务不应拥有呈现状态。

***

## 13. 用例（Use Case）规则

仅在用例能带来价值时才使用它。

合理的使用理由：

```text
涉及多个仓库
权限工作流
提权工作流
多步骤业务流程
回滚
可复用的领域操作
```

不要为每个单一的仓库方法都创建一个用例类。

***

## 14. DTO 与领域规则

切勿将原始 JSON 深入传递到 UI 层。

推荐流程：

```text
JSON
  ↓
DTO
  ↓
仓库 / 映射器（Mapper）
  ↓
领域模型（Domain Model）
  ↓
视图模型（ViewModel）
  ↓
视图（View）
```

避免 DTO 泄漏到呈现代码中。

***

## 15. 错误处理

不要向用户展示原始堆栈跟踪或异常字符串。

将错误规范化为 RemoteOS 的失败类型。

始终在日志中保留足够的技术细节以供诊断。

不要静默吞掉异常。

反例：

```dart
try {
  ...
} catch (_) {}
```

正确行为：

```text
捕获
  ↓
记录技术上下文日志
  ↓
映射到领域/呈现失败类型
  ↓
展示适当的用户可见状态
```

***

## 16. 安全规则

RemoteOS 管理服务器和特权操作。

切勿：

* 绕过授权来让功能「可用」；

* 硬编码凭据；

* 记录密码日志；

* 记录令牌日志；

* 记录私钥日志；

* 记录证书私钥材料；

* 静默提升权限；

* 图方便而弱化 TLS 验证；

* 未经显式配置就禁用服务器身份检查；

* 假设客户端权限检查足够。

客户端权限状态用于改善用户体验。

服务器始终是权威的。

***

## 17. 权限与提权规则

特权操作必须使用中心化的权限/提权基础设施。

不要为每个功能自行实现提权机制。

期望模式：

```text
命令
  ↓
用例 / 仓库
  ↓
权限检查
  ↓
如需要则发起提权请求
  ↓
用户决策
  ↓
服务端提权
  ↓
重试操作
```

不要自动接受提权提示。

***

## 18. 对话框（Dialog）规则

RemoteOS 使用自定义桌面模态框。

不要无理由地用任意 `showDialog()` 实现替换功能对话框。

优先使用：

```text
ModalHost
ModalCoordinator
功能对话框状态
```

视图模型可以请求对话框状态。

视图负责渲染它。

视图模型不应调用 Flutter 对话框 API。

***

## 19. 上下文菜单（Context Menu）规则

保持桌面上下文菜单行为。

UI 负责：

```text
位置
叠加层（overlay）
指针交互
动画
```

视图模型负责：

```text
选中项
启用状态
命令
带权限感知的动作可用性
```

***

## 20. 导航规则

RemoteOS 是面向工作区的。

不要将每次功能切换都建模为移动端的 `Navigator.push()`。

使用现有的壳（shell）/工作区基础设施。

不要在一个功能内创建第二套导航架构。

***

## 21. 窗口（Window）规则

桌面窗口行为是应用行为的一部分。

不要随意破坏：

```text
拖拽
缩放
最小化
最大化
还原
全屏
最小尺寸
已保存边界
自定义标题栏
```

窗口特定的状态不得成为应用范围的单例。

***

## 22. 服务器会话（Server Session）规则

如果会话所有权已经存在或可以明确化，则切勿引入隐藏的全局 `currentServer`。

服务器特定的依赖属于服务器会话范围。

示例：

```text
API 客户端
认证
权限
文件仓库
进程仓库
服务器事件
```

***

## 23. 本地化规则

复用已有的键。

当本地化键已存在时，不要硬编码面向用户的字符串。

在无关任务中不要重命名本地化键。

添加新字符串时：

1. 添加稳定的语义化键；
2. 添加到对应的资源文件中；
3. 更新所有必需的基线语言区域；
4. 不要将呈现文本放入业务逻辑中，除非它确实是领域数据。

***

## 24. 主题规则

不要硬编码属于 RemoteOS 主题系统的颜色。

使用主题令牌/扩展/组件。

仅当颜色由协议/内容在语义上固定时（而非外观层面），硬编码颜色才可接受。

示例：

```text
终端 ANSI 颜色
证书状态颜色模型
用户显式选择的颜色
```

即便如此，也优先使用命名的语义抽象。

***

## 25. Avalonia 迁移规则

从 Avalonia 迁移时：

不要机械地逐行翻译 XAML。

首先理解：

```text
布局意图
视图模型状态
命令
代码隐藏（code-behind）
对话框
上下文菜单
键盘行为
窗口行为
API 调用
主题资源
本地化资源
```

除非任务明确要求更改，否则保持原有行为。

***

## 26. 外部包规则

添加包之前：

1. 确认该功能是否已存在；
2. 确认该包确实能解决实际问题；
3. 优先选择专注的包而非单体框架；
4. 可行时将包相关代码隔离；
5. 避免由包驱动的架构变更；
6. 避免为相同的需求添加重复的包。

在功能任务中不要升级不相关的依赖。

***

## 27. 生成代码规则

除非生成器明确要求，否则不要手动编辑生成的文件。

将生成的代码与手写的功能逻辑隔离。

如果引入了代码生成器，请记录：

```text
存在的原因
如何运行
它拥有哪些文件
生成的文件是否提交入库
```

***

## 28. 格式化与静态分析

在认为代码变更完成之前：

```text
dart format
flutter analyze
相关测试
```

修复由你的变更引入的新警告。

除非有文档记录的理由，否则不要用宽泛的 ignore 来抑制分析器警告。

***

## 29. 测试期望

对于非平凡的逻辑，请添加或更新测试。

优先级：

```text
视图模型状态转换
命令行为
仓库映射
权限决策
错误映射
对话框状态
会话行为
```

当布局或交互行为很重要时，使用 widget 测试。

对关键的端到端流程使用集成测试。

***

## 30. 性能规则

避免不必要的整页重建。

优先使用细粒度的监听。

对于高频数据：

```text
终端
性能指标
传输进度
日志
```

优先使用流（streams）或专用缓冲区。

除非经过测量并证实合理，否则不要为每个高频事件反复克隆巨大的状态对象。

***

## 31. 生命周期规则

任何拥有以下对象的实体：

```text
ValueNotifier
StreamSubscription
AnimationController
FocusNode
ScrollController
Timer
WebSocket 订阅
Command
```

必须有明确的释放/生命周期所有者。

不要让订阅跨越功能/会话/窗口生命周期泄漏。

***

## 32. 日志规则

日志应帮助诊断：

```text
连接
认证
API 失败
会话状态
权限
功能操作
意外异常
```

不要记录密钥。

优先使用结构化上下文：

```text
serverId
feature（功能）
operation（操作）
requestId
sessionId
```

在可用且安全的情况下包含。

***

## 33. 注释规则

为以下内容编写注释：

```text
为什么
不变量
协议约束
平台特性
安全约束
迁移兼容性
非明显的变通方案
```

不要对显而易见的代码加注释。

反例：

```dart
// 设置 loading 为 true。
loading.value = true;
```

正例：

```dart
// 刷新期间保持之前的文件列表可见，以避免服务器往返过程中
// 出现完整的桌面列表闪烁。
```

***

## 34. TODO 规则

TODO 必须包含意图说明。

正例：

```dart
// TODO(remoteos-migration):
// 在证书仓库完全迁移完成后移除此适配器。
```

反例：

```dart
// TODO fix
```

不要用 TODO 替代对所需任务行为的实现。

***

## 35. 大型重构

对于大型重构：

1. 可行时在中间步骤保持可构建性；
2. 如需要创建兼容性适配器；
3. 一次迁移一项职责；
4. 保持提交/审查单元的内聚性；
5. 迁移完成后移除临时适配器。

除非明确要求，避免同时进行架构、UI、API 和命名重写。

***

## 36. Git 安全守则

不要：

* 改写历史；

* 强制推送；

* 删除分支；

* 移除用户工作成果；

* 重置不相关的变更。

不要丢弃非你创建的修改。

如果工作区中包含不相关的修改，请绕过它们继续工作。

***

## 37. 不要盲目删除「未使用」代码

RemoteOS 包含桌面/平台特定代码，这些代码可能不会从明显的路径被引用。

删除代码之前：

1. 搜索引用；
2. 检查动态注册；
3. 检查依赖注入（DI）注册；
4. 检查本地化/主题加载；
5. 检查平台分支；
6. 检查反射/序列化使用。

不要仅凭单次静态引用搜索就进行架构级删除。

***

## 38. API 兼容性

修改 API 模型时：

* 检查服务端代码；

* 检查 Avalonia 使用方；

* 检查现有 Flutter 使用方；

* 除非经明确授权，否则保持向后兼容；

* 同时更新 DTO 与测试。

不要臆造未记录的服务器字段。

***

## 39. UI 保真度

在迁移过程中，除非请求重新设计，否则保持现有的 RemoteOS 视觉层级。

优先保证：

```text
间距层级
控件分组
模态框行为
上下文菜单
桌面密度
导航结构
缩放行为
```

像素级完美渲染是可取的，但次于正确的桌面行为。

***

## 40. 溢出与桌面缩放

每个迁移后的桌面视图都必须考虑以下场景：

```text
最小支持宽度
典型宽度
最大化宽度
高 DPI
适用时的文本缩放
```

不要通过盲目将所有东西包进 `SingleChildScrollView` 来修复溢出。

使用适当的约束和响应式桌面布局。

***

## 41. 变更后报告内容

完成非平凡任务后，报告：

```text
变更了什么
变更的文件
重要的架构决策
运行的测试/分析
已知限制
如有，迁移差异
```

除非实际运行过，否则不要宣称测试通过。

***

## 42. 停止条件

如果发现以下情况，不要盲目继续：

```text
所需的 API 不存在
旧功能行为不明确
安全语义不一致
任务与架构规则冲突
工作区包含冲突的用户变更
```

可能时，选择最安全的兼容实现并记录限制。

不要编造缺失的 API 或行为。

***

## 43. 推荐的智能体工作流程

使用以下顺序：

```text
搜索
  ↓
阅读
  ↓
映射依赖
  ↓
实现最小的连贯变更
  ↓
格式化
  ↓
分析
  ↓
测试
  ↓
审查差异
  ↓
报告
```

***

## 44. 智能体最终规则

强制摘要：

```text
应当：
    先阅读架构
    保持现有行为
    保持文件小型化
    保持依赖方向正确
    使用仓库/服务
    中心化权限/提权
    保持主题/本地化
    测试重要逻辑
    迁移前检查 Avalonia

不应：
    在视图中调用 HTTP
    在视图模型中放置 BuildContext
    创建巨大的 Dart 文件
    使用隐藏的全局服务器状态
    绕过安全机制
    重新设计不相关的 UI
    静默移除功能
    臆造 API 契约
    随意重命名本地化键
    在功能开发中替换架构
```

首要原则：

> 在改变现有系统之前先理解它，然后做出最小的、符合架构的变更，保持 RemoteOS 行为不变。

